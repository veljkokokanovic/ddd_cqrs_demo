using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace UI.Gateway
{
    public static class HttpClients
    {
        public const string ProductApi = nameof(ProductApi);

        public const string OrderApi = nameof(OrderApi);

        public const string DeliveryApi = nameof(DeliveryApi);
    }
}
